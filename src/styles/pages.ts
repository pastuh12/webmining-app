import { Theme } from '@emotion/react';
import { SxProps } from '@mui/material';

export const pageGeneralStyles: SxProps<Theme> = {
  padding: 4,
  backgroundColor: '#f5f5f5',
  minHeight: '100vh',
};
